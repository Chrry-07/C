// write a C program to convert F to C and vice-versa
//  write a C program for swapping of two numbers using extra variable and without using extra variable
// to read a character and give the ascii value

#include <stdio.h>

int main() {
char c;
printf("enter c value: ");
scanf("%c", &c);

printf("\nthe character c contains %c", c);
printf("\n ASCII value of given character is %d", c);

return 0;
}
